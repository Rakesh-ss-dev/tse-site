name: Deploy to cPanel

on:
  push:
    branches:
      - main  # Triggers only when you push to the main branch

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Sync Files via SSH (Rsync)
        uses: Burnett01/rsync-deployments@7.0.0
        with:
          switches: -avzr --delete
          path: ./ 
          remote_path: /home/learnfundoo/public_html/tse/
          remote_host: ${{ secrets.REMOTE_HOST }}
          remote_user: ${{ secrets.REMOTE_USER }}
          remote_key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Run Remote Commands
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/learnfundoo/public_html/tse/
            # 1. Force remove the existing vendor folder to avoid scan errors
            rm -rf vendor
            # 2. Delete existing lock file to prevent conflicts
            rm -f composer.lock
            # 3. Re-install from scratch
            # We use --no-scripts to prevent any buggy triggers during install
            composer install --no-dev --optimize-autoloader --no-scripts